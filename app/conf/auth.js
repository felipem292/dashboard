import conf from "./config";
const auth = {
  mandatorySignIn: true,
  // region: conf.REGION,
  // userPoolId: conf.COGNITO.USER_POOL_ID,
  // identityPoolId: conf.COGNITO.IDENTITY_POOL_ID,
  // userPoolWebClientId: conf.COGNITO.APP_CLIENT_ID,
  region: "us-east-1",
  userPoolId: "us-east-1_1GqRU0GT0",
  userPoolWebClientId: "mh7llesb5ir20rh54sqhtc5su",
};
export default auth;
